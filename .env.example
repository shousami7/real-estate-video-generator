# ============================================================================
# Real Estate Video Generator - Environment Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# VIDEO GENERATION API AUTHENTICATION
# Vertex AI is used by default (uses GCP credits until they run out)
# ----------------------------------------------------------------------------

# DEFAULT: Vertex AI (Uses GCP Credits - ¥45,602 available)
# ==========================================================
# Set your GCP Project ID to automatically use Vertex AI
# The system will use this by default and consume your GCP credits first
#
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
#
# Benefits:
# - Automatically uses ¥45,602 GCP credits
# - Enterprise-level rate limits
# - More secure (service account authentication)
# - Better for production
# - Generates ~2,000 videos before credits run out

# FALLBACK: Google AI Studio (Only if Vertex AI is not available)
# ================================================================
# If you comment out GOOGLE_CLOUD_PROJECT above, the system will
# automatically fall back to Google AI Studio with API key
#
# GOOGLE_API_KEY=your-google-api-key-here
#
# Use cases:
# - When GCP credits are exhausted
# - Development/testing without GCP access

# ----------------------------------------------------------------------------
# CELERY & REDIS CONFIGURATION
# ----------------------------------------------------------------------------

# Redis URL for Celery task queue
REDIS_URL=redis://localhost:6379/0

# ----------------------------------------------------------------------------
# FLASK CONFIGURATION
# ----------------------------------------------------------------------------

# Flask secret key (change this to a random string in production)
SECRET_KEY=change-me-to-a-random-secret-key-in-production

# Flask environment (development or production)
FLASK_ENV=development

# ----------------------------------------------------------------------------
# OPTIONAL: GCP SERVICE ACCOUNT (For production deployments)
# ----------------------------------------------------------------------------

# Path to service account key file (if not using Application Default Credentials)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ----------------------------------------------------------------------------
# LOGGING
# ----------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
